%h1= t ("News")
- if current_user
  = link_to t ('New post'), new_post_path
- for post in @posts
  %hr
  %h1.title= post.title
  %p.byline
    = t ('Author')+':'
    = post.user.login.capitalize if post.user
    = post.created_at.strftime("%d.%m.%y %H:%M")
    - if current_user
      = link_to t ('Edit'), edit_post_path(post)
      = " | "
      = link_to t ('Delete'), post, :confirm => t ('Are you sure?'), :method => :delete
  %p
    = truncate( textilize( post.content ), :length => 1000 )
    %p.meta
      = link_to t ("Read more"), post_path( post )
      = t ('Comments')+':'
      = post.comments.count
= will_paginate @posts
