%h2= t("forgot_password")
= simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => {:method => :post}) do |f|
  = f.input :email
  = f.button :submit, t("send_reset_password_instructions")
= render :partial => "devise/shared/links"
